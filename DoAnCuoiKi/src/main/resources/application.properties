
vnpay.tmnCode=5X55ROE4
vnpay.hashSecret=1FX7JVG0AGL2NQBBIH4ZYXKKXIHPBK9B
vnpay.url=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
vnpay.returnUrl=http://localhost:8081/DoAnCuoiKi/payment/vnpay/return # Sẽ tạo controller xử lý
vnpay.ipnUrl=http://localhost:8081/DoAnCuoiKi/payment/vnpay/ipn     # Sẽ tạo controller xử lý (Cần server public để VNPAY gọi được)
# ============================================================
# 🌐 DATABASE CONNECTION - SQL SERVER (Windows Authentication)
# ============================================================

cloudinary.cloud_name=dzurcxze0
cloudinary.api_key=372615545721182
cloudinary.api_secret=um7sqvdnGeALDakq-CGI7lMZdeI

server.servlet.context-path=/DoAnCuoiKi

# URL kết nối SQL Server — sửa "demo_db" thành tên DB của bạn
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=AloTra;integratedSecurity=true;encrypt=true;trustServerCertificate=true

# Driver JDBC
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# Không cần username/password vì dùng Windows Authentication
# spring.datasource.username=
# spring.datasource.password=

# ============================================================
# ⚙️ JPA / HIBERNATE CONFIGURATION
# ============================================================

# Cập nhật schema khi có entity mới (chỉ dùng lúc học, dev)
spring.jpa.hibernate.ddl-auto=update


# Hiển thị câu lệnh SQL sinh ra
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Dialect cho SQL Server
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect

# Bật log chi tiết để kiểm tra SQL và giá trị binding
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# ============================================================
# 🌱 SPRING SETTINGS
# ============================================================

# Thymeleaf config
# Thymeleaf config
spring.thymeleaf.cache=false
# Dòng 13: Kiểm tra giá trị này (đặt comment trên dòng riêng)
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html


spring.servlet.multipart.max-file-size=10MB


spring.servlet.multipart.max-request-size=50MB

# Spring Security
#spring.security.user.name=admin
#spring.security.user.password=admin 


# Tên ứng dụng (hiện trong log)
spring.application.name=demo-sqlserver-windows-auth

# Cổng chạy ứng dụng

server.port=8081

jwt.secret=123123

# ============================================================
# 🔎 SQL INIT (tùy chọn) – có thể dùng để test connection
# ============================================================
# spring.sql.init.mode=always
# spring.sql.init.schema-locations=classpath:schema.sql
# spring.sql.init.data-locations=classpath:data.sql
